


class PineCone

    #default constructor.
    def __init__(self):
        self.array_of_seeds = []

    # add a seed. a seed is a list data type.
    def add_seed(self, seed):
        self.array_of_seeds.append(seed)

    #If the pinecone is populated with seeds, then it will not open. Only dispersion is allowed.
    def open(self):

    #once items are added this will encrypt everything and compress it. Only disperson is allowed post closing.
    def close(self):


    #disperse will spread seeds to intended target(s).
    def disperse(self):





